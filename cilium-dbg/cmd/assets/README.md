This directory stores YAML files that define CLI flags metadata.
They are used by cmd/preflight-k8s-valid-configmap.go to validate that user-provided configurations (ConfigMaps) only contain supported flags.
